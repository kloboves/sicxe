p2      start     3 * 1000
        float
        addr      A, B
        shiftl    T, 5
        shiftr    S, 3
add     add       =add
sub     sub       #sub
ldf     +ldf      @ldf
j       j         j + add + 30, X
        org       1000
equ     equ       *
equ     equ       equ
        use
        use       use
        ltorg
        base      base
        nobase
        extref    extref,extdef
        extdef    base
        byte      byte
        word      word
byte    byte      c'byte'
        resb      5
        resw      10
        end       p2
